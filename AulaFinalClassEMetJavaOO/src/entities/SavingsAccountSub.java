package entities;

public class SavingsAccountSub extends SavingsAccount {
	/*
	 *@Override
	 *public void withDraw(double amount) {
	 *	balance -= 2.0;
	 *}
	 *pode ser usado quando o termo final não aparece no método
	 *correspondente na superclasse, neste caso.
	 */
}
